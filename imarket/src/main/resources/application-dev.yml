server:
  port: 9090
  
# Hibernate JPA
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/iMarket_Refactor
    username: root
    password: victor626
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true